apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      component: web
  template:
    metadata:
      component: web
    spec:
      containers:
        - name: client
          image: rhtakealot/complex_client
          ports:
            - containerPort: 3000
